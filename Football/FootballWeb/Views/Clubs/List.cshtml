@model IEnumerable<ListClubResponse>

<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-4">
            <partial name="_ClubCard" model="@item" />
        </div>
    }
</div>

